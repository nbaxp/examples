name: portainer

services:
  portainer:
    image: portainer/portainer-ce:2.19.4
    restart: always
    ports:
      - 9000:9000
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:rw
      - /data/portainer:/data