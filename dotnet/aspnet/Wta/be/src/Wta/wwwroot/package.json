{
  "scripts": {
    "format": "biome format --write ./src",
    "lint": "biome check ./src",
    "prepare": "npm run format && npm run lint"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.8.3"
  }
}