# https://docs.docker.com/compose/how-tos/project-name/
COMPOSE_PROJECT_NAME=wta
NETWORK=${COMPOSE_PROJECT_NAME}_net
LAN=172.172.0
SUBNET=${LAN}.0/24
HUB=hub.hongtai-idi.com
DATA=./data
DATA2=./data2
LOGS=./logs
LOGS2=./logs2

# portainer
PORTAINER=${HUB}/portainer/portainer-ce:2.21.2
PORTAINER_IP=${LAN}.10

# nginx
NGINX=${HUB}/nginx:1.27.0
NGINX_IP= ${LAN}.21
NGINX_PORT_HTTP=80
NGINX_PORT_HTTPS=443

# minio
MINIO=${HUB}/bitnami/minio:2024
MINIO_IP=${LAN}.31
MINIO_PORT_UI=9000
MINIO_PORT_API=9001
MINIO_ROOT_USER=admin
MINIO_ROOT_PASSWORD=aA123456!

# redisinsight
REDISINSIGHT=${HUB}/redislabs/redisinsight:2.56.0
REDISINSIGHT_IP=${LAN}.40

# redis
REDIS=${HUB}/redis:7.4.0
REDIS_IP=${LAN}.41
REDIS_PORT=6379

# phpmyadmin
PHPMYADMIN=${HUB}/phpmyadmin/phpmyadmin:5.2.1
PHPMYADMIN_IP=${LAN}.50
PHPMYADMIN_PORT=5541

# mysql
MYSQL=${HUB}/mysql:8.4.2
MYSQL_IP=${LAN}.51
MYSQL_IP2=${LAN}.52
MYSQL_PORT=3306
MYSQL_PORT2=3307
MYSQL_ROOT_PASSWORD=aA123456!

# sql server\postgresql\oracle\mongodb  60 70 80 90 

# kafka
KAFKA=${HUB}/bitnami/kafka:3.8.0
KAFKA_UI_IP=${LAN}.110
KAFKA_IP=${LAN}.111
KAFKA_CONNECT_IP=${LAN}.119
DEBEZIUM_UI_IP=${LAN}.118

# doris
DORIS_FE=${HUB}/selectdb/doris.fe-ubuntu:3.0.0
DORIS_BE=${HUB}/selectdb/doris.be-ubuntu:3.0.0
DORIS_FE_IP=${LAN}.120
DORIS_BE_IP=${LAN}.121

# flink
FLINK=${HUB}/flink:scala_2.12
FLINK_IP=${LAN}.130
FLINK_IP1=${LAN}.131

# aspnet

ASPNET=${HUB}/aspnet:8.0
WTA_IP=${LAN}.201